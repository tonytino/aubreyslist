# Dependencies
node_modules/

# Build outputs
.output/
.vinxi/
.vercel/
dist/

# Test coverage
coverage/

# Stryker mutation testing (issue #187): the HTML/JSON reports and the sandbox
# directory Stryker copies the project into to run mutants. Both are regenerated
# by `pnpm test:mutation`; neither is committed.
reports/
.stryker-tmp/

# Playwright E2E artifacts
test-results/
playwright-report/
.last-run.json

# Environment variables
.env
.env.*
!.env.example

# Generated files
app/routeTree.gen.ts
app.config.timestamp_*

# Claude Code — ignore local/transient state (worktrees, local settings, plans),
# but DO version-control the shareable agent tooling so every clone/session gets it.
.claude/*
!.claude/skills/
!.claude/workflows/
