- `[manual]` Moved the listing-detail single-listing DB read out of the `/listings/$id` route into a client-callable `fetchListing` server fn (`app/server/listings/get-listing.fn.ts`, db impl in `get-listing.ts`), mirroring the `*.fn.ts` convention. The route loader no longer imports `getDb`/`drizzle-orm`/the `listings` table, so the `/listings/$id` route loader no longer pulls db into its own client graph during client-side navigation — the `findFirst` body now lives behind the `fetchListing` server fn (the #93 client-bundle-leak class). Pure refactor — same `notFound()` not-found path, response shape, and loader behavior (closes #122).
