- `[manual]` Listing-detail hero now shows a real Google Place photo with author attribution, fetched at render time through a server-side media proxy (`GET /api/places/photo`) — the Places key stays server-only, nothing Google-sourced is persisted (ADR-014), results ride a short in-process TTL cache, and a new `place_photos_enabled` AppSetting (default on) kill-switches the whole surface. Adds `@hono/zod-validator` (the standard Hono request validator per `docs/agents/api.md`).
