- `[manual]` Unified the claim-badge family sizing (AUB-224): the headline safety badge and the other claim badges now render at the EXACT same size and shape everywhere (including the detail hero, which no longer up-scales the headline) from one shared source (`BADGE_FAMILY_SIZE`). The headline is set apart only by its solid colour fill.
- `[manual]` Suggested claim badges now keep the attribute's own icon instead of a generic Sparkles glyph, and move the always-visible "AI" marker to AFTER the label (AUB-225). The gradient ring, the real always-painted "AI" text, its label-free tooltip trigger, and the "not yet confirmed by the community" gloss are all preserved.
