- `[manual]` Audit-polish follow-ups (no behaviour change): reworded the #116 changelog fragment so the SQL `fresh` predicate's `IS NULL` arm reads as a defensive alignment for an unreachable confirm-majority state (a confirm always sets `lastConfirmedAt`) rather than a real tier-4 ranking; added an honesty note to `sqlTierFor` in `browse.test.ts` flagging that the adjacent SQL-structure-pin test is what keeps the JS mirror from becoming a tautology; and promoted the `RecentIncidentBanner` from a passive `role="region"` landmark to a polite ARIA live region (`role="status"` / `aria-live="polite"`) so this safety-critical recent-harm warning is announced when it appears, with the colour+icon+text accessibility intact (#136).
