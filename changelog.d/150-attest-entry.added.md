- `[manual]` Restored the core confirm/dispute loop: listings can now be attested (#150, ADR-007). The listing-detail "Community claims" surface ALWAYS renders the full fixed GF taxonomy (all 7 attributes) as attestable — no more "Coming soon" dead-end — each with its visible confirm/dispute counts + recency (honest empty state for zero votes, never a fabricated rating) and the viewer's confirm/dispute controls (login-gated; anonymous sees the evidence + a sign-in affordance). Voting is now addressed by `(listingId, attribute)` rather than a pre-existing `claimId`: `castVote` creates the `claims` row LAZILY on the first vote via an `onConflictDoNothing` upsert on `claims_listing_attribute_unique` (idempotent, race-safe, no backfill, no empty rows), then upserts the attestation and recomputes `lastConfirmedAt` from the surviving confirms exactly as before; `retractVote` resolves the slot's claim without creating one (a no-op when none exists). The `requireCurrentUser()` → `enforceWriteLimit()` guard order (401/429 before any DB work) and the #41 moderation-visibility filter are preserved — `getListingClaimAggregates` now returns one entry per taxonomy attribute (existing claims merged + honest empty entries), scoped to `moderation_status = 'visible'` so hidden/removed claims never contribute, and the headline celiac-safe vs. gluten-friendly cue still derives only from the visible celiac attribute ("Not yet attested" when none). No schema change. Covered by server, component, and an updated lazy-create E2E spec (`tests/e2e/attest-claim.spec.ts`).
