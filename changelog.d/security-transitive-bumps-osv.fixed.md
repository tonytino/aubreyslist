- `[manual]` Bumped six vulnerable transitive dependencies flagged by the osv-scanner CI gate (12 advisories, incl. one critical in `tar` and a ReDoS pair in `brace-expansion`): `tar` 7.5.21, `postcss` 8.5.22, `js-yaml` 4.3.0, `fast-uri` 3.1.4, `brace-expansion` 2.1.2/5.0.8 (the `@5` security-floor override raised from 5.0.6 once 5.0.8 cleared the 7-day publish quarantine). Lockfile + override change only — no runtime code touched.
