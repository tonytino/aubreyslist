- `[manual]` Bumped six vulnerable transitive dependencies flagged by the osv-scanner CI gate (12 advisories, incl. one critical in `tar`): `tar` 7.5.21, `postcss` 8.5.22, `js-yaml` 4.3.0, `fast-uri` 3.1.4, and `brace-expansion` pinned to 5.0.8 across ALL majors — GHSA-mh99-v99m-4gvg affects every release below 5.0.8 and the 2.x line has no patched build, so the workspace override forces the fixed major everywhere (full test suite + production build validate the substitution). Lockfile + override change only — no runtime code touched.
