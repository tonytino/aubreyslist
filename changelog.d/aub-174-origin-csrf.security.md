- `[manual]` Added a defense-in-depth Origin/Referer same-origin check (on top of the `SameSite=Lax` session cookie) that rejects state-changing cross-origin requests with `403` before any DB work, enforced centrally on both the Hono `/api/*` surface and `createServerFn` POSTs (`app/server/security/origin.ts`). Env-aware (works on localhost and Vercel previews) with no new dependency.
