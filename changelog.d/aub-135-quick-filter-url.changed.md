- `[manual]` The directory's prebuilt quick filters (Celiac-safe / Gluten-friendly / Recently verified) are now URL-driven (`?quick=`) and applied server-side, so an applied chip persists across refresh, back/forward, and shared links, and the result count + pagination honestly reflect it (previously the chips lived in client state, vanished on rerender, and only refined the loaded page). The mapping to the displayed safety glance (ADR-007) is enforced by a "do not weaken" equivalence test.
