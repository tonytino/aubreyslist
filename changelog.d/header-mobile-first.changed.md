- `[manual]` Reworked the site header into one consistent, mobile-first layout at every breakpoint: a hamburger menu on the left, the brand wordmark centred, and the theme toggle + account menu on the right (verified down to 375px / iPhone SE). Simplified the signed-out CTA to a compact "Log in" button (generic Phosphor icon, no Google logo).
- `[manual]` Surfaced the admin-area link to moderators (labelled "Moderation"; admins still see "Admin"), since `/admin` is RBAC-gated and shows them only the moderation queue. Added a mobile-first design principle (375px minimum) to `docs/agents/styling.md`.
