- `[manual]` Upgraded the pinned package manager from pnpm 9.15.0 to pnpm 11.9.0 (required for the supply-chain settings above) and moved dependency `overrides` out of `package.json` — which pnpm 11 no longer reads — into `pnpm-workspace.yaml`. Re-resolving the lockfile under the 7-day release-age policy stepped `@sentry/*` and `lucide-react` down to their most recent aged versions (`@sentry/tanstackstart-react` floor relaxed to `^10.62.0`, `lucide-react` to `^1.21.0`; both float back up automatically once the newer releases age past the window) and refreshed other transitive deps to their newest ≥7-day-old versions.
