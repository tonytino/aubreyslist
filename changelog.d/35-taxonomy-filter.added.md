- `[manual]` Added GF taxonomy filtering to the browse list (`/listings`): URL-driven `?attrs=` checkboxes over the fixed 7-attribute taxonomy, filtered server-side and composable with text search via `and(...)`. A listing matches an attribute only when its claim has positive community consensus (confirms strictly outnumber disputes), never merely because a claim row exists. Pagination and total count reflect the active filters; honest "no matching listings" empty state.
