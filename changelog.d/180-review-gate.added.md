- `[manual]` Added an `adversarial-review` CI gate (`.github/workflows/pr-conventions.yml`) that enforces the orchestration.md review loop on every PR: it requires both the `review:adversarial-passed` label and a well-formed `## Adversarial review` body block (validated by `.github/scripts/check-review-block.mjs` — an `overall: SHIP` verdict or the escalation marker), with a `skip-review` label bypass for trivial/human-only changes.
