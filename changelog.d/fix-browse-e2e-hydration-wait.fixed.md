- `[manual]` Fixed a red `main`: two browse E2E specs (`browse sort control is labeled and drives the URL`, `filter and sort compose in the URL`) raced client hydration in the `pnpm dev` harness — they interacted with the sort `<select>` and filter checkbox before the client bundle finished loading, so the controls' URL-writing handlers weren't wired yet and the URL never updated. They now wait for hydration (`window.__TSR_ROUTER__`) before interacting, using a shared `waitForHydration` helper also adopted by `hydration.spec.ts`. Test-only; the sort/filter features themselves were correct.
