- `[manual]` Suggested (bot-provenance) claim badges — browse cards and the new listing-detail claim badge row — now use a subtler treatment (owner call): the attribute's own icon swaps for the `Sparkles` "AI-suggested" glyph inside a purple gradient ring, plus a compact always-visible "AI" tag next to it, replacing the old, longer "Suggested: " text prefix. The fuller "not yet confirmed by the community" gloss lives in a supplementary tooltip, but the "AI" tag itself is never hover/focus-gated — it stays reachable for touch-only users, who can never trigger the tooltip. The listing detail Claims tab's own "Suggested by Aubrey's Bot" chip picked up the same gradient-ring treatment for visual consistency across surfaces.
