- `[manual]` Added the canonical trust-model invariant suite (ADR-007/008, #178/#185): gated, property-style Vitest tests that pin the product's core safety guarantees so an agent change can't silently regress them — no secret scoring (the summary is a deterministic, field-for-field roll-up of visible evidence), a recent incident flags the listing regardless of older confirmations, staleness flags (never hides) a claim past the admin-tunable window, one attestation per user per claim (server upsert + the DB `UNIQUE(claim_id, user_id)` constraint), and Place ID as the dedup key with the reachable manual-entry fallback. DB-enforced invariants live in `tests/integration/trust-model.invariant.test.ts` (self-skip without `TEST_DATABASE_URL`); the DB-free ones in `app/trust/trust-model.invariant.test.ts` and `app/server/listings/intake-dedup.invariant.test.ts`.
