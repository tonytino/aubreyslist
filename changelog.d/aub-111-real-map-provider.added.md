- `[manual]` The directory Map view now renders a real Google map (AUB-111) via `@vis.gl/react-google-maps` when the public, referrer-restricted `VITE_GOOGLE_MAPS_BROWSER_KEY` is provisioned (AUB-217): Advanced Marker safety pins at true coordinates, bounds-fitted camera, a working recenter FAB (reduced-motion aware), and class-based dark-mode color scheme. Without the key, the stylized CSS-placeholder map renders unchanged, so keyless environments (CI, E2E, local dev) stay deterministic.
