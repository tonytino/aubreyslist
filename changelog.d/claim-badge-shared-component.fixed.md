- `[manual]` Listing detail page: non-headline claim badges (e.g. "Off-menu GF on request", "Dedicated fryer") were never rendered anywhere on the hero — only the headline celiac-safe/gluten-friendly badge showed. A new badge row below the hero's safety bar now shows every attribute with positive community consensus or a live bot suggestion. Extracted the shared `ClaimBadge` component (icon + label, taxonomy-driven) so this row and the browse cards' suggested badges render from ONE place instead of each hand-assembling its own icon+label chip.
