- `[manual]` Repaired the `main` typecheck break caused by the grouped dependabot zod 3→4 major (#307) + TanStack Router bumps landing together: `browseSearchSchema.page`/`.sort` gained explicit `.default(...)` alongside `.catch(...)` (zod 4 no longer infers catch-only params as optional inputs, which made typed `<Link to="/">`/`navigate` calls demand `page`/`sort`), the incident `note` schemas moved `.optional()` outside the normalising transform (zod 4 pipes otherwise force `note: undefined` on every caller), and a map-test VM fixture gained its required `confirmedAttributes` (a pre-existing gap from #289, unrelated to the bumps but caught by the same gate). URL-state runtime behavior is unchanged — `parse({})` still equals `BROWSE_SEARCH_DEFAULTS`, garbage still degrades per-param, and `stripSearchParams` still strips a default search to a clean `/`.
