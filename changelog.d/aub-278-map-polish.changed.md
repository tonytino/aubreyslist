- `[manual]` Map view pin + band polish (AUB-278, AUB-301 follow-up): a pin whose place has a recent incident on top of any non-incident headline — a place with no headline verdict yet included — now shows a small red incident badge on its dot — the dot keeps its headline colour, mirroring the mini-card's headline chip + incident add-on chip (screen readers already hear the incident in the pin's name). The grey "no verdict" dot gets a darker fill in dark mode so it no longer merges with its white halo. The bottom card carousel fades out at its right edge so a clipped card reads as "scroll for more" now that the scrollbar is hidden.
