- `[manual]` Fixed: hidden/removed listings no longer leak their claims/incidents via per-listing RPCs. `moderation_status` has no parent→child propagation, so the public per-listing/per-claim read paths now cross-check the parent listing's visibility: `listIncidents`, `getListingClaimAggregates` (incl. the viewer-vote query), and `getClaimAggregate` inner-join `listings` and require `listings.moderation_status = 'visible'`, so a moderated-away listing returns empty/zeroed aggregates and no incidents on its own addressable reads (no schema change — JOIN, not cascade).
