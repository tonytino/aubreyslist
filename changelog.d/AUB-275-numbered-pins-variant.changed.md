- `[manual]` Numbered-pins map preview variant (AUB-275): map pins render their 1-based entries-order number in the dot instead of the safety icon (same safety-state fill + `*-foreground` number colour, 24px dot in the 44px tap target), and each carousel mini-card leads its name row with the matching number chip on a neutral `secondary` fill — a sighted pin ↔ card correlation aid, built as a preview variant for visual comparison against the map-view baseline. The number never enters an accessible name, and the safety icon stays on the card's chip row.
