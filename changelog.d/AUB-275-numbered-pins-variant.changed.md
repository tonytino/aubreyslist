- `[manual]` Map view preview variant V1 (AUB-275): pins show their 1-based entries-order number instead of the safety icon (same safety fill + `*-foreground` number colour, 24px dot in the 44px tap target), and each carousel mini-card leads its name row with the matching number chip on the same fill — a sighted pin ↔ card correlation aid. The number never enters an accessible name; the safety icon stays on the card's chip row (the variant's deliberate tradeoff). Preview-only for owner comparison against the AUB-274 baseline — may never merge.
