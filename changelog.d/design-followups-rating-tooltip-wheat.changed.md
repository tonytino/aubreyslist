- `[manual]` Browse-card follow-ups from the design review: the external Google-rating pill now carries an ADR-007 tooltip ("… not an Aubrey's List safety score") on hover and keyboard focus — lifted out of the card's stretched link so it's an honest interactive `<button>` trigger while the whole card stays one tap target — and the gluten-friendly wheat-strike icon reads more clearly at chip size (wider strike cutout). Safety meaning stays exclusively in `SafetySignal`.
