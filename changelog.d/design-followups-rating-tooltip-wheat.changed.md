- `[manual]` Browse-card follow-ups from the design review: the external Google-rating and community-saves pills now carry an ADR-007 tooltip ("… not an Aubrey's List safety score") on hover and keyboard focus — lifted out of the card's stretched link so they're honest interactive `<button>` triggers while the whole card stays one tap target — and the gluten-friendly wheat-strike icon reads more clearly at chip size (wider strike cutout). Safety meaning stays exclusively in `SafetySignal`.
