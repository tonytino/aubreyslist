- `[manual]` Wired server-side Sentry (AUB-110): added `instrument.server.mjs` (root `Sentry.init` with the public DSN), a custom SSR entry `app/server.ts` that wraps the request handler with `wrapFetchWithSentry`, `app/start.ts` seating `sentryGlobalRequestMiddleware`/`sentryGlobalFunctionMiddleware` as the first global middleware, and the `sentryTanstackStart` Vite plugin for source-map upload. Production `--import`/source-map wiring is tracked separately (AUB-106).
