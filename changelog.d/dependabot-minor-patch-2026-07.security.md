- `[manual]` Raised dependency security floors alongside the Dependabot minor-and-patch group: `tar` `^7.5.21`, a new `fast-uri` `^3.1.4`, and `brace-expansion` floored per major line (`@2` `^2.1.2`, `@5` `^5.0.7`). Clears 8 of the 10 osv-scanner advisories; the remaining `GHSA-mh99-v99m-4gvg` needs `brace-expansion@5.0.8`, still inside the repo's 7-day `minimumReleaseAge` quarantine. Build/dev tooling only — no runtime dependency changed.
