- `[manual]` Raised dependency security floors alongside the Dependabot minor-and-patch group: `tar` `^7.5.21`, a new `fast-uri` `^3.1.4`, and a single `brace-expansion` `^5.0.8` — enabled by a new `minimatch` `^10.2.5` floor, since only minimatch 10 can consume brace-expansion 5's named `expand` export. The osv-scanner job goes from red on `main` to clean. Build/dev tooling only — no runtime dependency changed.
