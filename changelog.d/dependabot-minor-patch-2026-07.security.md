- `[manual]` Raised three osv-scanner security floors in the `pnpm-workspace.yaml` `overrides:` block alongside the Dependabot minor-and-patch group: `tar` `^7.5.16` → `^7.5.21` (GHSA-23hp-3jrh-7fpw, GHSA-8x88-c5mf-7j5w, GHSA-r292-9mhp-454m, GHSA-w8wr-v893-vjvp), a new `fast-uri: ^3.1.4` floor collapsing the 3.1.2/3.1.3 copies into one (GHSA-4c8g-83qw-93j6, GHSA-v2hh-gcrm-f6hx), and `brace-expansion` floored **per major line** — `@2: ^2.1.2` (new) and `@5: 5.0.6` → `^5.0.7` (GHSA-3jxr-9vmj-r5cp). The split is deliberate: brace-expansion v3+ is ESM-first and exports `expand` as a named export, while minimatch 5/9 (reached via `archiver` → `readdir-glob` / `glob@10`) `require()` the v2 CommonJS default export, so a blanket `^5` override would break them at runtime. Build/dev tooling only — no runtime dependency or app code changed.
- `[manual]` Fixed the lint errors the `@biomejs/biome` `^2.5.1` → `^2.5.4` bump surfaced: four new `lint/correctness/noUnsafeOptionalChaining` violations in `app/server/admin/set-role.test.ts` and `app/server/routes/auth.test.ts` (the mock-call payload is now captured into a typed local before it is dereferenced, matching the idiom already used elsewhere in those files — no assertion changed), plus three files reformatted by 2.5.4's revised `it.each(...)(...)` call formatting. `biome.json`'s `$schema` now points at the 2.5.4 schema.
