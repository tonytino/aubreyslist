- `[manual]` Browse list cards and the map carousel's mini-cards now show a real Google Place photo (with compact author attribution) when one is available, sourced by a new batched, concurrency-limited server function (`fetchBrowsePhotos`) that fetches at most `BATCH_PHOTO_CONCURRENCY` places at once and shares the listing-detail hero's per-Place-ID cache (AUB-215/ADR-014) — a place warmed by either surface is warm for both, so paging the directory costs at most one photos-only call per NEW place per 12h window. Photos load client-side after hydration (never in the route loader), so the initial browse paint is unaffected; the existing `place_photos_enabled` kill switch, a missing API key, or any per-listing failure all degrade silently to the existing gradient placeholder tile.
