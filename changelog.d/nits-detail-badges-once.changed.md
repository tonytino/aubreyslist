- `[manual]` Listing detail page: stopped showing the headline safety state twice (repo-owner feedback) — retired the standalone `SafetyBadges` row that duplicated the hero's cue below it. `SafetySummary`'s `"hero"` variant now owns the WHOLE safety-badge row for the hero card: the headline celiac-safe/gluten-friendly/stale badge (or the honest "Not yet attested" empty state) plus the recent-incident badge, each still keeping its colour + icon + text-label contract and supplementary tooltip.
- `[manual]` The hero's badge row now scrolls horizontally on overflow (`overflow-x-auto`, hidden scrollbar, `shrink-0` chips — the same pattern as the directory's `FilterChips` row) instead of wrapping, so it stays usable at the 375px minimum width. Compensating padding keeps each badge's keyboard focus ring un-clipped inside the scroll container, and when an unattested listing also has a recent incident the "Not yet attested" empty state compacts to a chip (guidance sentence in its tooltip) so the incident badge is never pushed off-screen.
