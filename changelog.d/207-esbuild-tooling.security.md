- `[manual]` Cleared the last three osv-scanner esbuild advisories in the dev-tooling chain: bumped `drizzle-kit` `^0.30.0` → `^0.31.10` (its direct esbuild moves to `^0.25.4`, clearing the 0.19.12 copy) and added surgical `pnpm.overrides` — `@esbuild-kit/core-utils>esbuild: ^0.25.0` (forces drizzle-kit's deprecated `@esbuild-kit` loader chain off esbuild 0.18.20, GHSA-67mh-4wv8-2f99) and `tsx>esbuild: ^0.28.1` (forces tsx's esbuild 0.27.7 up, GHSA-g7r4-m6w7-qqqr). Every esbuild copy is now `0.25.12` or `0.28.1`; no `<0.25.0` and no `0.27.x` remain. Dev/build tooling only — no runtime dependency or app code changed.
