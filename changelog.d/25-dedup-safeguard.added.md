- `[manual]` Hardened the add-listing duplicate safeguard (issue #25, ADR-008) in `app/server/listings/create.ts`: a Places-mode submission for an already-listed Place ID resolves to the existing listing (`created: false`) instead of erroring, and a manual-mode submission is now blocked when it matches an existing manual listing on a normalized name+address (trim, collapse whitespace, lowercase, fold diacritics, strip punctuation — `app/server/listings/dedup.ts`) with a structured `DuplicateListingError` carrying the existing listing's id/name so the UI can point the user to it.
