- `[manual]` Made incident-date normalization (`toCalendarDayString` in `app/trust/incident-recency.ts`) robustly timezone-correct: the Neon HTTP driver builds a Postgres `date` as a JS `Date` at LOCAL midnight, so the helper now reads it back with LOCAL getters (recovering the stored calendar day in ANY runtime TZ) instead of UTC getters, which were off-by-one on positive-offset runtimes (e.g. `Asia/Tokyo`). Corrected the misleading "UTC midnight" comments and added a unit test exercising the real driver shape (`new Date(2026, 5, 28)`) under a forced `Asia/Tokyo` TZ.
