- `[manual]` Accept `axe-core` and `@axe-core/playwright` (MPL-2.0) as reviewed, package-scoped license exceptions (#195). They are dev/test-time-only accessibility tooling (the a11y test lane), never shipped in the app bundle and not modified by us, so the MPL's per-file source-sharing obligation is not triggered — same rationale as the existing `lightningcss` exception. Turns the `License allowlist` CI gate green.
