- `[manual]` Documented the propagation-tag convention for this instance repo in `changelog.d/README.md`: default to `[manual]` for app-specific changes; use `[propagate]` only to flag a change worth upstreaming to the construct template. Clears up `[manual]` vs `[propagate]` ambiguity for a leaf instance.
