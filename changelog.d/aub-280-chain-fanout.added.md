- `[manual]` Added a chain-location fan-out to the seed pipeline: `pnpm db:seed:expand-chains` (plus an "Expand chain locations" workflow) enumerates the other in-radius locations of curated chains whose GF offering is corporate policy, baking them into a second generated file the seeder concatenates in. Expanded locations inherit only the brand-wide attribute subset (`chainWideAttributes`, curated on 18 chains), must pass a brand-name identity check, and carry no menu link — location-specific claims are never assumed to transfer.
