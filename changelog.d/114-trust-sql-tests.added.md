- `[manual]` Pinned the trust-critical SQL ↔ pure-JS-spec equivalence with behavioral unit tests (no live DB): the taxonomy filter's strict `confirms > disputes` direction (`filter.test.ts` — a tie and a dispute-majority are NOT matched, only a confirm-majority is, and multiple attributes compose as AND), a shared `(confirms, disputes, lastConfirmedAt) → tier` case table asserting the SQL `buildOrderBy` safety-tier CASE produces the SAME ordering as `safetyTierRank`/`deriveHeadlineSafetyState` for fresh-safe > stale > contested > unattested (`browse.test.ts`), and that the incident edit/retract WHERE filters by BOTH `id` AND `userId` so an id-only regression fails (`incidents/index.test.ts`) — so future drift between the SQL and the explainable spec can no longer ship green (#114).
