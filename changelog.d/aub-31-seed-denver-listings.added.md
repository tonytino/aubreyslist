- `[manual]` Added a `pnpm db:seed` command + a "Seed production database" GitHub Action that seed the directory with ~45 curated, real Denver-metro gluten-free / celiac spots (resolved to live Google Place IDs), each carrying one or more GF-attribute labels **suggested by a curator bot ("Aubrey's Bot")**. Idempotent (dedup on Place ID); see `docs/agents/database.md`.
- `[manual]` Added `claims.suggested_by` so a bot-suggested label renders a "Suggested by Aubrey's Bot" badge on the listing detail + directory cards, stays out of the community confirm/dispute counts (ADR-007), and clears automatically the moment a real user attests the label.
