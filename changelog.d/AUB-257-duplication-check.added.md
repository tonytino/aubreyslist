- `[manual]` Added a whole-repo copy-paste duplication check (`pnpm duplication`, jscpd) that scans `app/`, `db/`, `scripts/` at `minTokens: 70` and fails PRs on repeated code blocks. Runs as its own **Duplication** CI job; NOT part of `preflight`. Test files are excluded (baseline: 8 clones in source code only; 149 of 164 found clones were test-to-test, which repeat by design). Config lives in `.jscpd.json`; see `docs/agents/tooling.md` for semantic-duplication handling via review loop.
