- `[manual]` Added a whole-repo copy-paste duplication check (`pnpm duplication`, jscpd) that scans `app/`, `db/`, `scripts/`, and `.github/scripts/` at `minTokens: 70` and fails PRs on repeated code blocks. Runs as its own **Duplication** CI job; NOT part of `preflight`. Test files are excluded (a sweep at `minTokens: 50` found 164 clones, 149 of them test-to-test, which repeat by design; at the shipped `minTokens: 70` with tests excluded the baseline was 8 clones, all resolved before the gate was switched on). Config lives in `.jscpd.json`; see `docs/agents/tooling.md` for semantic-duplication handling via the review loop.
