- `[manual]` Raised the mutation-testing floor with real tests, not a lower threshold: the incident calendar-date parser now pins both regex anchors (a timestamp or a date with text around it is not a calendar day) and the year half of its round-trip overflow check (`0099-01-01` must not silently parse as 1999); `toCalendarDayString` pins that a full ISO timestamp is reduced to its bare calendar day rather than passed through (the #45 failure mode); `editIncidentInputSchema` gets the date-validation coverage the report schema already had (past/today accepted, future and impossible dates rejected with the message that matches what the reporter typed), closing a gap where the edit path's no-future rule was entirely untested; and `findRecentIncident` pins that it returns the NEWEST in-window incident from an unsorted list.
- `[manual]` Pinned the exact bucket boundaries of both relative-time formatters (`formatRelativeTime`, the browse card's compact chip) so an age landing exactly on an edge promotes to the coarser unit — no "60 minutes ago", "24h", or a two-day-old confirmation described as "yesterday".
- `[manual]` Pinned that the manual-entry dedup matcher never blocks a listing as a duplicate merely because two names (or two addresses) both normalize to empty, that `deriveListingTrustGlance` treats an `undefined` celiac aggregate exactly like a `null` one instead of throwing, and that `enforceWriteLimit` really enforces the per-user cap while never metering an anonymous caller (plus a non-empty 429 message, which is the response body the caller sees).
