- `[manual]` Extracted the browse route's URL-param helpers (`parseAttrs`, `serializeAttrs`, `coordsFromSearch`) and the `BROWSE_PAGE_SIZE`/`MAX_PAGE_SIZE` constants into a new client-safe `app/listings/browse-params` module (no db import), single-sourced and shared by the route and the server validator; added unit tests and tightened the route's `?lat=`/`?lng=` search schema with `.finite()` to match the server `coordsSchema`.
