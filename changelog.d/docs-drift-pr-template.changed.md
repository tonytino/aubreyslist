- `[manual]` Fixed stale contributor docs in `docs/agents/tasks.md`: the "Updating the Changelog" section now describes the `changelog.d/` fragment workflow (pointing to `changelog.d/README.md`) instead of the retired `CHANGELOG.md [Unreleased]` edit, "Handing Off for Review" now references the `.github/pull_request_template.md` that auto-populates PR bodies, and both `tasks.md` and `AGENTS.md` note that web sessions use the GitHub MCP tools while local sessions use the `gh` CLI.
