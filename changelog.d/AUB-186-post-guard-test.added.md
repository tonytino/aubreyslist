- `[manual]` Added a static-analysis Vitest test (`tests/unit/server-fn-post-guards.test.ts`, helpers in `tests/unit/server-fn-post-guards.helpers.ts`, issue AUB-186) that enumerates every `createServerFn({ method: "POST" })` export under `app/server/**` and asserts it calls an auth guard from `app/server/auth/guards.ts` (`requireCurrentUser`/`requireCurrentRole`/`requireUser`/`requireRole`), either directly in the `*.fn.ts` file or one import hop away in the delegated implementation module (`./index`, `./actions`, `./set-role`, ... — this repo's established seam). A documented `EXCEPTIONS` map (currently empty) is the only way to exempt a POST fn, and an exception without a non-empty reason fails its own test. Zero new dependencies; the detector itself is unit-tested against synthetic guarded/unguarded fixtures, including a regression case proving it correctly flags an unguarded fn.
