- `[manual]` Map view foundation (AUB-274): pins slimmed to 24px micro-dots (same colour + icon + accessible label, inside a tap target grown to 44px), the carousel mini-cards dropped the decorative accent block for slim text-dense cards that follow the browse card's trust-row rules (shared `SafetySignal` chip, incident add-on chip, shared dashed empty-state badge), and pin ↔ card selection now syncs both ways — selecting a pin scrolls its card into view and selecting a card pans the live map to its pin (zoom untouched, reduced-motion safe, never fighting the refit-on-filter logic). Map pin icons now come from `SafetySignal`'s state config (`safetyIcon`), one source of truth for the state → shape mapping.
