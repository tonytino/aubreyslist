- `[manual]` Documented the URL-state convention (shareable/restorable UI state — filters, sort, search, page, tab — lives in the URL via `validateSearch` + `stripSearchParams` + `navigate`; only truly ephemeral state stays in `useState`) as a Hard Rule in `AGENTS.md` and a new `docs/agents/url-state.md`, and added a "URL-state hygiene" dimension to the adversarial review loop (`docs/agents/orchestration.md` + `.claude/workflows/adversarial-review.mjs`).
