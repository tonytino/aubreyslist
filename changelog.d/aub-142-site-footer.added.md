- `[manual]` Added a site footer (`app/components/SiteFooter.tsx`), mounted in `AppShell` (`app/routes/__root.tsx`) so it renders on every route: the brand wordmark, a footer nav linking the site's real existing pages (Browse, Add a listing, Favorites, About), and a copyright line. The legal-links slot (privacy/terms/disclaimer/moderation/contact) is deliberately reserved but left unpopulated in a commented placeholder — those routes don't exist yet, and wiring them now would 404 (AUB-142).
