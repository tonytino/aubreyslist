- `[manual]` Redesigned the restaurant directory (`/listings`) to the Claude Design bundle: sticky header, server-side free-text search across all listings, quick filter chips, a List/Map toggle with safety-colored map pins projected from real coordinates, and honest loading / empty / no-results states. Server taxonomy filtering, sorting, and "near me" distance remain available via the Filters sheet.
