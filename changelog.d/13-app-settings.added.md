- `[manual]` Added an admin-tunable app-settings / feature-flag system (`app/server/settings/`): a typed key/value registry over the `app_settings` table with codec-based serialize/parse (string, int, and string-union), in-code defaults so reads never fail on an empty table, and an idempotent `seedDefaults`. Seeds the first two consumers — `intake_mode=places` (ADR-008) and `staleness_months=6` (ADR-007).
- `[manual]` `getSetting`/`setSetting` are fully typed per key (no `any`); `setSetting` is a plain server-side write left as a clean admin-guard seam for issue #17 (`requireRole('admin')`) to wrap once it lands, rather than forking an auth check.
