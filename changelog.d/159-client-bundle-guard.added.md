- `[manual]` Added a mechanical client-bundle guard to the `build-smoke` CI job that greps the browser assets (`.output/public/_build/assets/`) for db/neon tokens (`@neondatabase`, `neonConfig`, `drizzle-orm/neon-http`, `DATABASE_URL`) and fails the build if any are found — so a future `.fn.ts` seam edit or plugin regression that drags neon/drizzle into the client bundle (the #93/#122/#141 bug class) is caught for ALL seams, present and future, instead of relying on each seam's import pattern (#159).
