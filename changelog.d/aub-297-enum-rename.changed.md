- `[manual]` Renamed the persisted `claim_attribute` enum value `celiac_safe_vs_gluten_friendly → celiac_safe` (AUB-297), via a data-preserving enum-recreate migration that rewrites existing claim rows. Internal only — the label was already "Celiac-safe", so nothing user-visible changes; an old shared link carrying `?attrs=celiac_safe_vs_gluten_friendly` drops that filter token instead of erroring.
