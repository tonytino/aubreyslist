- `[manual]` Added the claim confirm/dispute attestations layer (`app/server/attestations/`): login-gated `castVote` (confirm/dispute) and `retractVote` server functions enforcing one vote per user per claim — a repeat vote upserts via the `attestations_claim_user_unique` constraint (`onConflictDoUpdate`, never a duplicate) and a retract deletes the row. A `confirm` bumps `claims.lastConfirmedAt` to now so the recency/staleness signal stays current; dispute and retract leave it untouched. Exposes a typed `getClaimAggregate` helper (confirm/dispute counts derived from the visible attestation rows plus `lastConfirmedAt`) for the transparent trust summary (#29) to consume — a roll-up of visible evidence, never a hidden score (ADR-007). Writes leave a clearly-marked rate-limit seam for issue #18 to wrap, rather than inventing a limiter.
