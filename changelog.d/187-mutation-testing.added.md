- `[manual]` Added Stryker mutation testing on the trust-critical modules — `app/trust/**`, the manual-entry dedup matcher (`app/server/listings/dedup.ts`), and the write rate limiter (`app/server/rate-limit/**`) — wired as `pnpm test:mutation` and a path-filtered `mutation.yml` workflow that hard-fails when the mutation score drops below the break threshold. Measured baseline on main is 76.99%; `thresholds.break` is set to 76 (rounded down) so it passes today and can be ratcheted up as tests improve (#187, part of #178).
