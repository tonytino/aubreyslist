- `[manual]` The "Filter listings" bottom sheet (and its Filters chip + checkbox panel) is retired — its taxonomy filters, sort control, and pager all moved to always-visible surfaces (AUB-198). The near-duplicate headline "Celiac-safe" taxonomy chip is not shown next to the quick Celiac-safe chip; `?attrs=` links carrying the headline attribute still work and surface a removable chip (the attribute's retired enum key is dropped from old links instead).
