- `[manual]` Thread the listing-detail loader-resolved `now` (reconstructed from `nowMs`) into the headline safety derivation and the community-claim rows instead of letting them call a fresh `new Date()` at render time, so the headline celiac-safe/gluten-friendly cue and each claim's recency + staleness roll-up use the same server-resolved instant on SSR and after hydration — no flicker or off-by-one at the staleness-window edge (closes #115).
