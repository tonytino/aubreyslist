- `[manual]` Fixed the add-listing places search getting stuck with no results (`/listings/new`). A failed search that *threw* (transport/uncaught server error) fell through to a silent "No matches found" state instead of showing an error, and re-submitting the same term never re-ran because the cached result stayed fresh under the app's 60s `staleTime`. Search now surfaces a retryable error on failure and re-fetches on every Search submit.
