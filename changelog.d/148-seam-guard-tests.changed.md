- `[manual]` Added seam-level auth + per-user write-limit guard tests for the add-listing/places `createServerFn` seams (`app/server/listings/create.fn.ts` `submitCreateListing`, `app/server/places.fn.ts` `autocompletePlaces`) — the call-sites the intake forms actually run after #141 — asserting anonymous → 401 (impl not invoked), over-limit → 429 (impl not invoked), and `requireCurrentUser` before `enforceWriteLimit` before the impl via `invocationCallOrder`, restoring the regression tripwire that the pre-#141 tests left on the now-orphaned wrappers. Also dropped the unused `getPlaceDetails` export from `places.fn.ts` (no client imports it; place-details resolution happens server-side inside `runCreateListing`). Test-only; no app-behavior change (#148, follow-up to #141).
