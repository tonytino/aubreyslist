- `[manual]` Softened the Google photo attribution overlays (hero + browse card): smaller text (`text-caption` -> `text-[11px]`) and lower opacity (`text-white/90` -> `/80` on the hero, `/85` on the card), with the card's bottom scrim nudged from `/60` to `/65` to keep the pairing above WCAG AA 4.5:1 contrast in the worst case (a pure-white photo). Attribution text and Google-required legibility are unchanged.
