- `[manual]` Added a DB-level schema-constraint integration suite (`tests/integration/schema-constraints.test.ts`) that applies the migrations to a real Postgres and asserts the runtime invariants the metadata-only unit suite can't catch: `UNIQUE(claim_id, user_id)` on attestations, the `flags_one_target` CHECK (zero/two targets rejected, exactly-one accepted), `place_id` uniqueness (with coexisting NULLs), and `onDelete: cascade` from a listing to its claims/incidents/flags. It self-skips (`describe.skipIf`) unless `TEST_DATABASE_URL` is set, so `pnpm preflight`/CI stay green with no database; fixtures use a unique per-run token and `afterAll` cleanup so it is idempotent against the persistent CI Neon branch.
