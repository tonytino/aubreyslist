- `[manual]` ADR-014: Google Maps Platform usage & content policy — storage limits (place_id forever, lat/lng ≤30 days, never persist Google photos/ratings/hours/reviews), render-time server-side fetch with short-TTL in-process cache, attribution rules, server/browser key split (`VITE_GOOGLE_MAPS_BROWSER_KEY`, AUB-217), per-SKU cost ceilings with graceful degradation (`place_photos_enabled` kill-switch, deep-link fallback, AUB-218 Enterprise-field switch). Revises v1's "no embedded map — deep-link only" decision (owner direction 2026-07-06); user photo uploads stay deferred. Updated `docs/agents/domain.md`, `docs/product/overview.md`, and `docs/agents/environment.md` to match.
