- `[manual]` Cleared two High-severity transitive advisories whose fixes had aged past the release-age quarantine: `brace-expansion` 5.0.8 → 5.0.9 (GHSA-rgw5-rvv9-x895) and `fast-uri` 3.1.4 → 3.1.5 (GHSA-7p8r-x3mc-p8w7), both by raising their security floors in `pnpm-workspace.yaml`. The `brace-expansion` ↔ `minimatch` pairing invariant was re-verified by resolving the tree rather than by a green build (as that file's comment requires): the lockfile carries a single deduped `brace-expansion@5.0.9` and `minimatch` remains on the 10.x line. Also closed a governance gap — `pnpm-workspace.yaml` and `osv-scanner.toml` are now owner-gated in `.github/CODEOWNERS`, so weakening the quarantine, lowering a security floor, or accepting a vulnerability can no longer ship without the owner's review.
