- `[manual]` Bumped three vulnerable transitive dependencies flagged by the osv-scanner CI gate (8 advisories): `browserslist` 4.28.8 (GHSA-73wf-gq98-2v4g, GHSA-c83g-rgw3-j3cx, 7.5 — a new override floor), `fast-uri` 3.1.6 (four advisories, 7.5), and `qs` 6.16.0 (GHSA-4mjr-xmp4-gh2g, GHSA-x5fp-wj9c-mxmx, 6.3). Every fix had already cleared the 7-day release-age quarantine, so the gate promoted them from deferred warnings to hard errors on its own. Lockfile + override floors only — no runtime code touched, and no `minimumReleaseAgeExclude` fast-track or osv-scanner waiver added.
