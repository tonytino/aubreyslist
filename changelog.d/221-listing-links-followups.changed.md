- `[manual]` The edit-links dialog is a full-screen takeover on mobile (AUB-221): full viewport height (dvh) and width with square corners below `sm`, the title and save/cancel actions always visible, and only the fields area scrolling internally — no more clipped header/actions on a phone. At `sm+` the centred dialog returns, height capped at 85dvh with the same internal-scroll split.
- `[manual]` The menu link kind now renders a restaurant-menu document icon (lucide `BookOpenText`) instead of the `Menu` hamburger, which read as app navigation (AUB-221).
- `[manual]` Retired the one-time "Backfill production listing links" GitHub Action after its successful prod run (AUB-221); `pnpm db:backfill:listing-links` remains for local/dev databases.
- `[manual]` The seed pipeline writes typed menu links (AUB-220): an entry's `menuUrl` is seeded as a `menu`-kind `listing_links` row (`created_by` null) instead of the legacy `listings.menu_url` column, and only for listings the run itself inserted — a re-seed never resurrects a menu link a user removed, and future seed runs can no longer mint legacy rows.
