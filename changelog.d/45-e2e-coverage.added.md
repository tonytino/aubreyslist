- `[manual]` Added Playwright E2E coverage for the key pilot flows (#45): seeded browse + GF taxonomy filter (celiac-safe + dedicated fryer), mocked Google sign-in (a sealed session cookie minted with the repo's own `sealSessionPayload`), add-a-listing (authenticated manual intake), attest-a-claim (confirm → trust summary updates), and report-an-incident (flags the listing summary). A shared `tests/e2e/fixtures.ts` seeds via Drizzle with unique per-run tokens and full teardown, and the CI E2E step now also supplies `SESSION_SECRET`; the auth/DB specs self-skip when the CI E2E database/session secret are absent.
