- `[manual]` Unified every per-claim chip onto ONE shared `ClaimChip` primitive (icon + label at `BADGE_FAMILY_SIZE`): the listing-detail `ClaimBadge`, the add-listing review outcome chip, AND the interactive confirm/dispute vote toggle now render through the same component, so the toggle IS the shared badge in an interactive wrapper (via Radix `Slot`/`asChild` onto a real `<button>`) rather than a look-alike kept in visual sync. The vote toggle's behaviour and a11y are unchanged (icon + visible label + `aria-pressed`, celiac-safe/gluten-friendly/incident pressed fills, disabled + anonymous states, toasts). The "Suggested by Aubrey's Bot" provenance ring stays de-duplicated behind the one `SuggestedRing` primitive. Extended the parity-guard test to fail if the review chip, the detail badge, OR the vote toggle drift on icon/label or family size (AUB-227).
