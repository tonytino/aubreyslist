- `[manual]` Rendered the static claim chips from the shared badge family: the add-listing review outcome chip now composes the same `Badge` shell + `BADGE_FAMILY_SIZE` as the detail-page `ClaimBadge`, the "Suggested by Aubrey's Bot" provenance chip is de-duplicated behind one shared `SuggestedRing` gradient primitive, and the confirm/dispute vote toggle aligns to the same size/shape tokens (behaviour unchanged). Added a parity-guard test that fails if the review chip and the detail badge drift on icon/label or family size (AUB-227).
