- `[manual]` Added "got glutened here" incident reports (`app/server/incidents/`): a login-gated, rate-limited `reportIncident` server function (required `occurredOn` date, optional severity + note), a `listIncidents` read that returns a listing's incidents most-recent-first, and pure recency helpers (`isRecentIncident` / `findRecentIncident`) keyed off a 90-day `RECENT_INCIDENT_WINDOW_DAYS` constant — deliberately separate from the 6-month claim-staleness window (#31). On the listing detail page (`app/routes/listings.$id.tsx`) a recent incident now raises a prominent warning banner near the top (colour + warning-triangle icon + text label) regardless of older confirmations — fresh harm is never buried (ADR-007) — and the "Incident reports" slot lists every report with date and optional severity/note plus a submission form for signed-in visitors. The banner/recency logic is reusable for the browse list-card signal that lands with #33.
