- `[manual]` Added Dependabot (`.github/dependabot.yml`) for GitHub Actions + npm/pnpm: weekly updates with a 7-day release `cooldown` (supply-chain guard so yanked/compromised brand-new releases aren't adopted immediately), grouped minor/patch PRs with majors kept separate, SHA-pins preserved for actions, and a `skip-changelog` label so its PRs satisfy the changelog-fragment CI gate. Resolves the Node 20 action-deprecation warning by keeping `actions/checkout` + `actions/setup-node` current.
