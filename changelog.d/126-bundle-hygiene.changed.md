- `[manual]` Moved the GF taxonomy attribute list into a new client-safe `app/listings/taxonomy` module (a plain `as const` tuple + the derived `ClaimAttribute` type, no drizzle import) and had `db/schema.ts` derive its `claim_attribute` pgEnum from it (single source of truth, enum values unchanged — no migration). The browse route, `TaxonomyFilter`, and the browse param helpers now import the constant from there instead of the drizzle-backed `claimAttributes` in `~/db/schema`, so the taxonomy no longer drags drizzle/neon/`DATABASE_URL` into the shared client bundle (#126).
