- `[manual]` Replaced the check-in-a-circle brand glyph with a custom ear-of-wheat mark struck through by a single diagonal cut — the "gluten" symbol with a strike wiped out of it as negative space (an SVG mask cutout, not a drawn line), so the gap shows the background through: gluten, struck out. Applied to the header `Wordmark` and `public/favicon.svg` (plus the apple-touch/PWA/OG images).
