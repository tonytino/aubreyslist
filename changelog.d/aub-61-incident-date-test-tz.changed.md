- `[manual]` Fixed a timezone-sensitive unit test in `app/server/incidents/index.test.ts` ("normalizes a driver-returned Date occurredOn"): it modeled the Neon driver as returning a UTC-midnight `Date`, but the driver (pg-types, OID 1082) builds a Postgres `date` at LOCAL midnight — so under a negative-offset `TZ` (e.g. `America/Denver`) the test read back the prior calendar day and failed. The test now models the driver faithfully with a local-midnight `Date` (`new Date(2026, 5, 28)`), matching the sibling `Asia/Tokyo` regression test; the (already-correct) production normalization is untouched.
