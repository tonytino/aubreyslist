- `[manual]` Added best-effort per-user write rate-limiting infrastructure (`app/server/rate-limit`) — a reusable in-memory token-bucket-style guard (synchronous `enforceRateLimit` for Hono routes, async `enforceWriteLimit` for server functions) that throws `HTTPException(429)` past a configurable burst cap (default 50 writes/60s), ready for add-listing / attest / report-incident to adopt.
