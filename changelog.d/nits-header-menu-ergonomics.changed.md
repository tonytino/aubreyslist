- `[manual]` Header menus are now comfortable on touch devices: the shared dropdown primitives give every menu item a >= 44px hit height, larger text, and a wider icon-label gap on coarse pointers (`pointer-coarse:`), while desktop keeps the compact sizing; menu panels gain viewport-edge collision padding.
- `[manual]` The hamburger nav and avatar account triggers grow to a 44px hit area on touch, both header menu panels widen on touch so labels breathe at 375px, and Sign out sits behind a separator with its full padded row now submitting the sign-out POST (previously only the inner label area did).
