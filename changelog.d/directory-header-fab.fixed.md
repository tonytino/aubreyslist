- `[manual]` Fixed the restaurant directory's duplicate header (removed the redundant wordmark from the directory toolbar — the app-shell header already brands the page) and the floating action button: renamed "Add a spot" → "Add listing" and anchored it to the fixed-height app-shell so it stays pinned to the column's bottom-right instead of scrolling into and overlapping the cards.
