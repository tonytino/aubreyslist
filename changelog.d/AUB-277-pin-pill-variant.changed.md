- `[manual]` Map view preview variant V3 (AUB-277): the selected map pin expands sideways into a name pill (safety fill + icon + truncated restaurant name, brand ring, still 24px tall) instead of scaling up, on both map render paths. Preview-only for owner comparison against the AUB-274 baseline — may never merge.
