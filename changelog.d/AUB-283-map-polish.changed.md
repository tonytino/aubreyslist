- `[manual]` Map view polish (AUB-283): tapping a pin now reliably scrolls the bottom carousel to that restaurant's card (a direct scroll on the carousel replaces `scrollIntoView`, which mobile Safari mishandled) and the selected card lands flush with the map's left edge. Each card gains a chevron that opens the listing (muted until selected, purple once selected), tapping an already-selected card opens it too, the second line shows distance when available (address otherwise), bot-suggested unattested cards show the "Suggested by Aubrey's Bot" hint, and the carousel ends with a spacer so the last card can scroll clear of the Add listing button.
