- `[manual]` Every fresh agent session now bootstraps as an orchestrator with zero human prompting: a `CLAUDE.md` preamble + SessionStart hook + `/orchestrate` skill drive the full lifecycle (Linear recon → workers by model tier → adversarial review → labeled PR → `safe:agent` self-merge / `safe:human` handoff).
- `[manual]` Documented the `safe:agent` self-merge runbook, the `safe:human` reviewer policy (the session owner reviews; owner-gated surfaces go to the repo owner per `docs/agents/governance.md`), the AskUserQuestion preference, and the tiny-task exception in `docs/agents/orchestration.md`; added a human-facing `CONTRIBUTING.md`.
