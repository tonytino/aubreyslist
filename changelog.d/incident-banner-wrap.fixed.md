- `[manual]` Fixed the recent-incident banner's pill wrapping to three lines on mobile (screenshot-confirmed). The pill now shows only "Recent incident" (`whitespace-nowrap`, always single-line); the relative recency ("2 days ago") moved into the banner's body sentence next to the absolute date, which wraps naturally. The whole banner stays one polite ARIA live region, so screen readers still announce both the incident and its recency together.
