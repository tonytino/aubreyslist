- `[manual]` Aligned the staleness boundary between the SQL browse sort/filter (`buildOrderBy` in `app/server/listings/browse.ts`) and the displayed glance (`isStale` in `app/trust/summary.ts`): both now derive their cutoff from a single shared `stalenessCutoff` helper, with an INCLUSIVE edge (`>=`) so a confirmation exactly on the staleness boundary is classified identically (fresh) in SQL and JS — no drift between the card and the DB ordering.
- `[manual]` Aligned the SQL `fresh` predicate's NULL-recency arm (`IS NULL OR >= cutoff`) with `isStale(null) = false` (ADR-007). This is a defensive alignment for an unreachable state: a confirm-majority always has a `lastConfirmedAt` (every confirm sets it), so the `IS NULL` arm can't actually be hit by a confirm-majority claim — it exists so the SQL and JS paths stay structurally identical rather than to produce a real tier-4 ranking.
- `[manual]` Guarded the admin `staleness_months` setting to a POSITIVE integer via a `positiveIntCodec`: a zero/negative/non-integer stored value now falls back to the 6-month default (`DEFAULT_STALENESS_MONTHS`) so a bad admin value can never break staleness.
