- `[manual]` Moved the listing detail page's "Report an incident" submission form into a modal opened by a "Report an incident" button, instead of rendering it always-expanded — an empty, fully-open form read like an active incident/alert on the listing until you noticed it was just a form. The incident list, owner edit/retract controls, and the signed-out sign-in gate are unchanged (AUB-102).
