- `[manual]` Added a sign-in-gated "Saved" filter to the directory: `?saved=1` drives a server-side `savedOnly` browse mode that constrains to the viewer's visible saved spots *before* paginating, so `page`/`total`/`hasMore` stay honest across the full favorites set (not a page slice). Anonymous clicks open the sign-in dialog; the `savedOnly` response is per-viewer/private and never shared/edge-cached.
