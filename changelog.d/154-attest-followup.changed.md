- `[manual]` Hardened the #41 moderation-visibility filter on the claim aggregate (#154, review follow-up to #150): corrected a misleading docstring in `getListingClaimAggregates` (moderation is SOFT — `UPDATE moderation_status`, never a delete — so the empty-entry fallback is load-bearing on the `moderation_status = 'visible'` predicate, not on any deletion), added the same `visible`-only predicate to the viewer-vote query as defense-in-depth (behavior-neutral; the votes were already gated by the visible-only map), and noted that `castVote`'s `lastConfirmedAt` recompute is non-atomic on neon-http but self-healing. No behavior change.
