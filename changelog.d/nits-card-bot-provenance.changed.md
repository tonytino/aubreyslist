- `[manual]` Browse cards now label EVERY listing with live curator-bot suggestions "Suggested by Aubrey's Bot" (owner nit 7) — the label is provenance, no longer gated on "no real celiac evidence" — and render a lavender + Sparkles badge for each bot-suggested claim attribute (icon + label), clearly distinct from real evidence signals (ADR-007: a suggestion never reads as a community-confirmed verdict).
- `[manual]` Moved the "Suggested by Aubrey's Bot" label from the card's safety row into the bottom meta row's freshness slot (owner nit 8), so bot-suggested cards read uniformly with verified ones; a real freshness cue wins the slot (evidence over provenance) and the AUB-194 equal-height reservation is preserved.
- `[manual]` Removed the "saves" pill from browse cards (owner nit 10): the pill markup, the `saveCount` VM field, and the now-unused server-side `getFavoriteCounts` aggregate/`favoriteCount` plumbing. The Google-rating pill and the heart save button are unchanged.
