- `[manual]` Browse cards now label EVERY listing with live curator-bot suggestions "Suggested by Aubrey's Bot" (owner nit 7) — the label is provenance, no longer gated on "no real celiac evidence" — and render a lavender + Sparkles badge for each bot-suggested claim attribute (icon + a visible "Suggested:"-prefixed label), so a suggestion is readable as a suggestion by text alone and never mirrors a real verdict chip (ADR-007). The batched suggestion query is additionally vote-gated (`NOT EXISTS` attestations), so a claim caught in a vote's non-atomic suggestion-clear window can never badge as suggested.
- `[manual]` Moved the "Suggested by Aubrey's Bot" label from the card's safety row into the bottom meta row's freshness slot (owner nit 8), so bot-suggested cards read uniformly with verified ones; a real freshness cue wins the slot (evidence over provenance) and the AUB-194 equal-height reservation is preserved.
- `[manual]` Dropped the visible "saves" word from the browse card's save-count pill (owner, PR #274): the pill is now a compact heart glyph + number, with the meaning carried by the glyph, an "N saves" accessible name, and the existing ADR-007 tooltip. The Google-rating pill and the heart save button are unchanged.
