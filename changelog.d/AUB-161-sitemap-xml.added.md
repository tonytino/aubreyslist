- `[manual]` Added a dynamic `/sitemap.xml` (`app/routes/sitemap[.]xml.ts`) listing the static public pages (`/`, `/about`) plus an absolute `/listings/$id` URL for every listing with `moderationStatus = 'visible'`, so search engines can discover listing detail pages without crawling the SPA. Implemented as its own Server Route (not a Hono mount) to avoid touching `app/server/index.ts`.
