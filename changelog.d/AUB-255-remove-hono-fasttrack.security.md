- `[manual]` Removed the temporary `hono@4.12.34` entry from `minimumReleaseAgeExclude` in `pnpm-workspace.yaml` (AUB-255). It fast-tracked the CVE-2026-69207 fix past the 7-day release-age quarantine on 2026-08-04; that version cleared the window on 2026-08-10, so the carve-out was dead weight. `minimumReleaseAgeExclude` is now empty — the healthy state, since every entry is a deliberate hole in the quarantine. Removing it does not move `hono`, which stays resolved at 4.12.34; the lockfile is unchanged.
