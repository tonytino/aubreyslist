- `[manual]` Accessibility gate (#192): an always-on, DB-free CI lane runs axe-core (WCAG 2 A/AA) against the public static pages (`/`, `/about`, `/style-guide`) on every PR and fails on any violation; DB-backed routes are covered by the existing DB-gated E2E lane.
