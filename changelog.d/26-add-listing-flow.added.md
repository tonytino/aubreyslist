- `[manual]` Added an authenticated add-listing flow at `/listings/new` (`app/routes/listings.new.tsx`): the active intake mode (`getSetting('intake_mode')`, ADR-008) drives the surface — `places` shows Google Places search-and-pick, `manual` shows a name/address/lat-lng form — with an optional menu-link URL field (no uploads in v1). On success it redirects to the listing detail page (`/listings/$id`); anonymous visitors get a sign-in prompt.
- `[manual]` Added the listing-create server function (`app/server/listings/create.ts`): gated server-side by `requireCurrentUser()`, validated with a Zod discriminated union, resolves canonical name/address/coords server-side from the Places provider in `places` mode, and dedups gracefully on the unique `place_id` — a duplicate routes the user to the existing listing instead of erroring. A clearly-marked rate-limit seam (issue #18) wraps the write once that infra lands.
