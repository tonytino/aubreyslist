- `[manual]` Map view variant preview V2 (AUB-276, one of three candidates compared on Vercel previews — not intended to merge as-is): the carousel mini-cards lead with a 1-based index badge in the current entries order (decorative for AT — `aria-hidden`, never part of the accessible name), and the selected card's badge mirrors the selected pin dot's `ring-brand/50` halo on a solid AA-safe fill so the pin ↔ card pairing reads as one matching brand treatment. Pins are unchanged — they keep their full colour + icon + label safety signals.
