- `[manual]` Bumped four vulnerable dependencies flagged by the osv-scanner CI gate (6 advisories): `hono` 4.13.5 (GHSA-crvj-82cr-hjcx, GHSA-g6gw-c38x-mqfc, GHSA-gqvv-2mrq-wpjv), `vitest` 4.1.11 and `@vitest/mocker` 4.1.11 (GHSA-82fw-gwwq-j7x9, both follow the same release), and `js-yaml` 4.3.2 (GHSA-2883-xcg3-v3hh, a new override floor). Every fix had already cleared the 7-day release-age quarantine, so the gate promoted them from deferred warnings to hard errors on its own. `hono` and `vitest` are direct dependencies and moved within their existing `^` ranges; `js-yaml` is transitive and needed a new `pnpm-workspace.yaml` floor. Lockfile + one override line only — no runtime code touched, and no `minimumReleaseAgeExclude` fast-track or osv-scanner waiver added.
