- `[manual]` Redesigned the listing card so every card reads as the same object: browse cards and map cards now share one six-part layout — photo, name, location, signals, divider, activity — that keeps its shape whether a spot has five badges or none, the heart now carries the save count instead of a second pill beside the name, map cards show the full activity row including the happy-patron count, and the "Food photo" placeholder caption is legible again in dark mode.
