- `[manual]` Redesigned the listing card so every card reads as the same object: browse cards share one layout — photo, name, location, signals, divider, activity — that keeps its shape whether a spot has five badges or none, and map cards share that same layout minus the photo. The heart now carries the save count wherever it appears, the listing page included, instead of a second pill beside the name; map cards show the full activity row including the happy-patron count; and the "Food photo" placeholder caption is legible again in dark mode.
