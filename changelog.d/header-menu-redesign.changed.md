- `[manual]` Redesigned the site header around one `sm` (640px) breakpoint. Below `sm` the primary nav and account controls consolidate into a SINGLE right-anchored combined menu (`app/components/SiteMenu.tsx`) with grouped **Navigate** (Browse / Add a listing / About) and **Account** (identity, Favorites, role-gated Admin/Moderation, theme toggle, Sign out — or Log in / preview-only Dev sign-in when signed out) sections; the wordmark is left-aligned and the left hamburger is gone. At `sm:`+ the layout splits back to inline, directly-reachable nav links ("Add a listing" as a brand-purple CTA), a standalone theme toggle, and the avatar account menu — today's behaviour.
- `[manual]` Folded the theme toggle INTO the mobile combined menu as a non-closing row (`onSelect` preventDefault keeps the menu open); it shares a new `useThemeToggle` hook with the standalone `sm:`+ button so the icon + "Switch to dark/light theme" label never drift. Meaning is carried by the glyph shape (Moon/Sun) AND text, never colour alone.
- `[manual]` The combined-menu trigger reads as a menu: signed-in shows the avatar + burger glyph (`aria-label="Open menu, signed in as {name}"`), signed-out is a generic menu icon (`aria-label="Open menu"`); both are >= 44px touch targets and the `<nav aria-label="Primary">` landmark persists in both layouts.
- `[manual]` Extracted the signed-in account rows (identity, Favorites, Admin/Moderation, sign-out POST) and signed-out auth rows into shared render helpers in `UserMenu.tsx`, plus the primary nav list into `nav-items.ts`, reused by both the mobile `SiteMenu` and the desktop `UserMenu` so the two surfaces can't diverge. The sign-out `form method="post" action="/api/auth/sign-out"` and the moderator+ role gate are preserved exactly.
