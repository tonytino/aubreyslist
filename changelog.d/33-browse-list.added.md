- `[manual]` Added the browse list with at-a-glance trust (#33) — the default Denver discovery view (domain.md → Discovery, list-first). A new `/listings` index route renders a page of scannable listing cards, each showing the headline celiac-safe vs. gluten-friendly state (colour + icon + text label via `SafetySignal`, never colour alone) and a recent "got glutened" incident flag at a glance, then links to the listing-detail page; the home "Browse Denver listings" CTA and the header "Browse" nav now point at it. A new client-safe pure module (`app/trust/browse-glance.ts`) derives each card's glance by reusing #29's `deriveHeadlineSafetyState` over the `celiac_safe_vs_gluten_friendly` claim aggregate and #30's `findRecentIncident` recency helper — honest by construction (no celiac claim / no evidence → an explicit "Not yet attested" empty state, never a fabricated verdict; a recent incident flags the card regardless of older confirmations). The batched loader (`app/server/listings/browse.ts`, client-callable via `browse.fn.ts`) assembles a page with NO N+1: a paginated alphabetical page of listings plus a total count, then ONE grouped celiac-aggregate query and ONE incidents query scoped to that page's listing ids, with the admin-tunable `staleness_months` AppSetting and a single server-resolved "now" threaded through the derivation so the glance matches the detail page (no SSR/client drift). Pagination lives in the URL (`?page=`); honest empty state when no listings exist yet. Unit tests cover the pure glance derivation (incl. no-evidence, dispute-majority, stale, admin-window cases), the server aggregation (celiac-safe/not-yet-attested/recent-incident/old-incident + pagination + empty-page short-circuit), and the card rendering (each state, empty state, recent-incident flag, detail link); a Playwright smoke test covers the route and the home CTA.
