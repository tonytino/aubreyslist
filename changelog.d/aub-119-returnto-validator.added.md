- `[manual]` Google sign-in now honors a `returnTo` target: `/api/auth/google` validates it (same-origin relative paths only — rejects `//`, `/\`, protocol-relative, absolute, CRLF, and `%2f%2f`-style encoded escapes; query strings preserved), stashes the accepted value in a short-lived httpOnly cookie beside the existing OAuth transaction cookies (never via Google's `state`), and the callback redirects there (default `/`).
