- `[manual]` Split the Denver seed so `pnpm db:seed` is now **API-free**: it inserts baked data committed in `scripts/seed-listings.generated.json` and never calls the Google Places API. A separate `pnpm db:seed:refresh` captures Places data once (from the human-curated `scripts/seed-sources.ts`) into that committed file. The production seed workflow no longer needs `GOOGLE_PLACES_API_KEY` — only a database URL. A new **"Refresh seed data"** GitHub Action re-bakes the JSON on demand (the only step that spends Places calls), and schema-PR **preview** databases are now seeded with the baked data right after they're migrated.
