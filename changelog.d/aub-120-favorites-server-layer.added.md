- `[manual]` Added the favorites server layer (`app/server/favorites/`): `addFavorite`/`removeFavorite` (auth + rate-limit gated, visible-listing-only, idempotent via the `(user, listing)` unique constraint), `getViewerFavoriteIds` (anonymous → `[]`, visible-only INNER JOIN), and the public user-agnostic `getFavoriteCounts` aggregate — all behind a client-safe `favorites.fn.ts` seam and a `favoriteInputSchema` (no `db` reachable from client imports).
