- `[manual]` Fixed: Places-mode dedup no longer returns hidden/removed listings — the duplicate-Place-ID lookups in `app/server/listings/create.ts` now filter `moderation_status = 'visible'` (mirroring the manual path), so a re-submitted Place ID whose listing was moderator-hidden/removed no longer leaks the full moderated row + a `created: false` moderation-state oracle to signed-in users. A Place ID mapping only to a moderated row is treated as absent (falls through to insert; the `place_id` UNIQUE + `onConflictDoNothing` still prevent a real duplicate), and the otherwise-confusing conflict-with-a-moderated-row case surfaces a clear, non-leaky "this place can't be added right now" error instead of the hidden row (#41).
